/**
 * MOCK: ids de alimentos que el usuario ha marcado como favoritos.
 * Hoy vive solo en el cliente (useState en la página de Favoritos).
 * Reemplazar por llamadas al backend (por ejemplo GET/POST/DELETE
 * /favorites) cuando exista autenticación real.
 */
export const MOCK_FAVORITE_FOOD_IDS: number[] = [1, 3];
